sudo lshw -class network
cd /etc/pm/sleep.d
sudo touch config
sudo gedit config
add to file
SUSPEND_MODULES= "PRODUCT_NUMBER"
echo "options PRODUCT_NUMBER fwlps=N" or sudo tee /etc/modprobe.d/PRODUCT_NUMBER.conf
